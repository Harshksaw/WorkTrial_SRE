[project]
name = "worktrial-sre"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi[standard]>=0.139.0",
    "uvicorn>=0.49.0",
]
